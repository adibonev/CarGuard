# CarGuard 🚗

Уеб приложение за управление на автомобилни услуги и напомни за подновяване.

## 🎉 Версия 2.0 - Supabase Migration Complete!

**Нова архитектура**: Пълна миграция от Railway backend към Supabase serverless!

## Функционалности

- 🔐 **Аутентификация** - Email/Password + Google OAuth (Supabase Auth)
- 👤 **Потребителски профили** - Управление на reminder настройки
- 🚗 **Управление на автомобили** - Добавяне, редактиране, изтриване
- 🔍 **VIN декодиране** - Автоматично попълване на данни от VIN номер
- 🛡️ **Управление на услуги**:
  - Гражданска отговорност
  - Винетка
  - Технически преглед
  - КАСКО
  - Данък МПС
  - Пожарогасител
  - Ремонти и обслужване
- 📧 **Автоматични напомени** - Email напомени преди изтичане (конфигурируемо)
- 📊 **Dashboard** - Визуално показване на статуса, графики, календар
- 👨‍💼 **Admin панел** - Статистики и управление на потребители

## Технологии

### Frontend
- **React 18** - UI библиотека
- **React Router** - Маршрутизация
- **Supabase JS Client** - Database & Auth
- **Recharts** - Графики
- **CSS3** - Стилизиране

### Backend (Serverless)
- **Supabase Auth** - Аутентификация (JWT + OAuth)
- **Supabase PostgreSQL** - База данни с Row Level Security
- **Supabase Edge Functions** - Serverless функции (Deno)
- **Resend API** - Email услуга

### Deployment
- **Netlify** - Frontend hosting (https://car-guard.netlify.app)
- **Supabase** - Backend infrastructure

## Инсталация

### Requirements
- Node.js (v18 или по-нова)
- Supabase account
- Resend API key (optional за email)

### Локално стартиране

1. **Клониране на проекта**
```bash
git clone https://github.com/adibonev/CarGuard.git
cd CarGuard
```

2. **Client setup**
```bash
cd client
npm install
```

3. **Конфигуриране на .env.local**
```bash
REACT_APP_SUPABASE_URL=your_supabase_project_url
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key
```

4. **Стартиране на приложението**
```bash
npm start
```

Приложението ще бъде достъпно на `http://localhost:3000`

## Database Setup

SQL schema е в `supabase-schema.sql`. Всички таблици и RLS policies са вече deploy-нати.

Таблици:
- `users` - Потребителски профили
- `cars` - Автомобили
- `services` - Услуги и дати
- `accounts` - Допълнителни account данни

## Admin Setup

За създаване на администраторски акаунт:

```sql
-- В Supabase SQL Editor
UPDATE users 
SET is_admin = TRUE 
WHERE email = 'your-admin-email@example.com';
```

След това можеш да влезеш в `/admin` с твоя email и password.

## Email Reminders

Edge Function `check-reminders` се изпълнява автоматично на всеки 6 часа чрез Supabase Cron.

Настройка:
1. Edge Function е deploy-нат
2. Добави `RESEND_API_KEY` в Supabase Edge Function secrets
3. Enable Cron Trigger: `0 */6 * * *`

## Архитектура

```
┌─────────────────┐
│   Netlify       │
│  (React App)    │
└────────┬────────┘
         │
         │ Supabase JS Client
         │
┌────────▼──────────────────────┐
│        Supabase               │
│  ┌─────────────────────────┐  │
│  │  Auth (JWT + OAuth)     │  │
│  └─────────────────────────┘  │
│  ┌─────────────────────────┐  │
│  │  PostgreSQL + RLS       │  │
│  └─────────────────────────┘  │
│  ┌─────────────────────────┐  │
│  │  Edge Functions (Deno)  │  │
│  │  - check-reminders      │  │
│  └─────────────────────────┘  │
└───────────────────────────────┘
         │
         ▼
   ┌──────────┐
   │  Resend  │ (Email)
   └──────────┘
```

## Security

- ✅ Row Level Security (RLS) на всички таблици
- ✅ Supabase Auth JWT токени
- ✅ HTTPS only (Netlify + Supabase)
- ✅ Environment variables за secrets
- ✅ Google OAuth 2.0

## Links

- **Live App**: https://car-guard.netlify.app
- **Admin Panel**: https://car-guard.netlify.app/admin
- **GitHub**: https://github.com/adibonev/CarGuard

## Migration Notes

Виж [MIGRATION_COMPLETE.md](MIGRATION_COMPLETE.md) за пълни детайли за миграцията от Railway към Supabase.

## License

MIT
