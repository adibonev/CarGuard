# 🚗 CarGuard - Как да стартираш сайта

## Способ 1: Най-лесен (препоръчано) ⭐⭐⭐

### Windows
1. Отвори `start.bat` файла (двойно кликване)
2. Браузърът ще се отвори автоматично
3. Готово! Сайтът е на `http://localhost:3000`

### macOS/Linux
```bash
chmod +x start.sh
./start.sh
```

---

## Способ 2: Ръчно (ако горното не работи)

Отвори **3 отделни терминала** в папката на проекта:

### Терминал 1 - Backend сервър
```powershell
npm start
```
Очаква да видиш: `Server running on port 5000`

### Терминал 2 - Frontend сервър
```powershell
cd client
npm start
```
Очаква да видиш: `Compiled successfully!`

### Терминал 3 - Браузър (opcional)
Отвори: http://localhost:3000

---

## Как да презаредиш сайта

### Option A: Автоматично
1. Затвори всички терминали (Ctrl+C)
2. Отвори `stop.bat` (ако ти трябва бързо)
3. Отвори `start.bat` отново

### Option B: Браузър F5 refresh
1. Натисни **F5** в браузъра (освежава только frontend)
2. Ако не работи, затвори таб и отвори отново `http://localhost:3000`

### Option C: Hard refresh (изчиства кеш)
- **Windows/Linux**: `Ctrl + Shift + Delete`
- **macOS**: `Cmd + Shift + Delete`

---

## Решаване на проблеми

### "Port 3000 is already in use"
```powershell
# Kill existing processes
taskkill /F /IM node.exe
```
После отново `start.bat`

### "Port 5000 is already in use"
Същото като горе - изтрий всички node процеси.

### "Module not found"
```powershell
npm install
cd client && npm install && cd ..
```

### Браузърът не се отваря
Отвори ръчно: `http://localhost:3000`

---

## Полезни команди

```powershell
# Спри backend
Ctrl + C (в терминал 1)

# Спри frontend
Ctrl + C (в терминал 2)

# Спри всички Node процеси
taskkill /F /IM node.exe

# Виж дали портовете са заети
netstat -ano | findstr :3000
netstat -ano | findstr :5000
```

---

## Архитектура

```
http://localhost:3000  ← React Frontend
    ↓
http://localhost:5000  ← Node.js Backend
    ↓
database.sqlite        ← SQLite Database (локален файл)
```

---

## Готини връзки

- Frontend код: `/client/src`
- Backend код: `/routes`, `/models`, `/services`
- Стилове: `/client/src/styles`
- Database: `./database.sqlite`

---

**Сега можеш да разработваш без помощ! 🎉**
